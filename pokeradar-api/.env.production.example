# Production Environment Variables for PokeRadar API
# Copy this to Railway's environment variables dashboard
# DO NOT commit actual values to git!

# Server Configuration
NODE_ENV=production
PORT=3000

# MongoDB Connection
# Get this from Railway MongoDB or MongoDB Atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/pokeradar

# Google OAuth Configuration
# Get from: https://console.cloud.google.com/apis/credentials
# Make sure to add your production domain to authorized origins
GOOGLE_CLIENT_ID=your_google_client_id_from_cloud_console
GOOGLE_CLIENT_SECRET=your_google_client_secret_from_cloud_console
GOOGLE_CALLBACK_URL=https://api.yourdomain.com/auth/google/callback

# JWT Configuration
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=generate_a_secure_random_string_at_least_32_characters_long
JWT_EXPIRES_IN=7d

# CORS Configuration
# Set to your frontend production URL
CORS_ORIGIN=https://yourdomain.com

# Rate Limiting
# 15 minutes window with max 100 requests
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
