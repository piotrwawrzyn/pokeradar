{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build:shared && npm run build -w pokeradar-scrapper && npx patchright install chrome"
  },
  "deploy": {
    "startCommand": "node pokeradar-scrapper/dist/app/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
