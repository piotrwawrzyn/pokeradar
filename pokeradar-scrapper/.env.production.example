# Production Environment Variables for PokeRadar Scraper
# Copy this to Railway's environment variables dashboard
# DO NOT commit actual values to git!

# Logging
LOG_LEVEL=info

# Shop Engine Filter
# Options: "cheerio", "playwright", "all"
# - "cheerio": Fast, lightweight scraping (good for simple sites)
# - "playwright": Full browser automation (for complex/JavaScript sites)
# - "all": Use both engines as configured per shop
SHOP_ENGINE=all

# MongoDB Connection
# IMPORTANT: Must be the SAME database as the API and notifications
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/pokeradar

# Retry Configuration
# Maximum number of retry attempts for failed requests
MAX_RETRY_ATTEMPTS=1

# NOTE: Patchright/Playwright Chrome installation happens automatically
# during Railway build via: npx patchright install chrome
